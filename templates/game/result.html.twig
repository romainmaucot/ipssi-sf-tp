{% extends 'base.html.twig' %}

{% block body %}

    {{ result.tirage }}
    {% dump(result) %}
    {% for row in result %}

        {{ row }}
    {% else %}
        <span colspan="5">no records found</span>
    {% endfor %}

{% endblock %}